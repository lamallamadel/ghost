apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghost-marketplace
  namespace: project-ghost-deployments
  labels:
    app: ghost-marketplace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ghost-marketplace
  template:
    metadata:
      labels:
        app: ghost-marketplace
    spec:
      serviceAccountName: ghost-marketplace
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ghost-marketplace
          image: ghcr.io/atlasia/ghost-marketplace:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: ghost-deployments-config
            - secretRef:
                name: ghost-deployments-secret
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: data
              mountPath: /data
      volumes:
        - name: tmp
          emptyDir: {}
        - name: data
          persistentVolumeClaim:
            claimName: ghost-marketplace-data
