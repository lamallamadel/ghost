apiVersion: v1
kind: ConfigMap
metadata:
  name: ghost-deployments-config
  namespace: project-ghost-deployments
data:
  NODE_ENV: "production"
  PORT: "3000"
  DB_PATH: "/data/marketplace.db"
  UPLOAD_DIR: "/data/uploads"
  S3_BUCKET: "ghost-extensions"
  S3_REGION: "us-east-1"
  S3_USE_SSL: "true"
